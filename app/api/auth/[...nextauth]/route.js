import { handlers } from "@/auth";

export const { GET, POST } = handlers // handlers are basically api points that were 
                                      // created by nextauth and we are going to export 
                                      // them to make them available
